APP_ENV=dev

# ── Default model shown in the picker. Users can switch per request in the UI.
LLM_PROVIDER=claude-opus-4-8
LLM_MODEL=claude-opus-4-8

# ── Anthropic (Claude) ───────────────────────────────────────────────────
# Get a key: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ── OpenAI (ChatGPT) ─────────────────────────────────────────────────────
# Get a key: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ── Google (Gemini) ──────────────────────────────────────────────────────
# Get a key: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# ── Ollama (local models) ────────────────────────────────────────────────
# Default reaches the host's Ollama from inside Docker.
# On Linux you may need to set this to http://172.17.0.1:11434 instead.
OLLAMA_BASE_URL=http://host.docker.internal:11434

# ── Infrastructure ────────────────────────────────────────────────────────
DATABASE_URL=postgresql+asyncpg://acme:acme@postgres:5432/acme
SYNC_DATABASE_URL=postgresql://acme:acme@postgres:5432/acme
REDIS_URL=redis://redis:6379/0
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=acme
KEYCLOAK_CLIENT_ID=acme-assistant
KEYCLOAK_CLIENT_SECRET=
DEMO_AUTH_FALLBACK_ENABLED=false
MCP_SERVER_URL=http://mcp-server:8001
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
OTEL_SERVICE_NAME=acme-app
CONFIRMATION_HMAC_SECRET=dev-only-secret-change-me
DEBUG_ENDPOINTS_ENABLED=true
